//TODO: 

//for in avail to use

//grab 

// A user should be able to click on different squares to make a move.
// Every click will alternate between marking an X and O
// Upon marking of an individual cell, use JavaScript to add a class to each cell to display the separate players.
// A cell should not be able to be replayed once marked.
// You should not be able to click remaining empty cells after the game is over.
// Add a reset button that will clear the contents of the board.
// Display a message to indicate which turn is about to be played.
// Detect draw conditions (ties/cat's game)
// Detect winner: Stop game and declare the winner if one player ends up getting three in a row.
// Hint: Determine a set of winning combinations. Check those combinations on the board contents after every move.

//5-10
  // capture DOM elements to store as vars

  // create game logig vars and conditions

  // Function boxClick --> add listener on user click of game tile
    /// check for x or o amd display nessage in message box
    // chcek of there have been 9 turns but no winner -  call tied game 

  // Function: gameOver() --> once weve had more than 4 moves 
  // check and see if there is a wonner on every turn
  // if player has won run stopGame()
      //send message to display message and turn div

  //functopm stopGame() --> doesmt allow for further game play once won

  //function resetGameboard() -->  add evemt listernt to #rest button on click
    //loops through gameboar; reasets vars

 
